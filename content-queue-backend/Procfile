web: pip install poetry && export PATH="$HOME/.local/bin:/root/.local/bin:$PATH" && poetry install --no-interaction --no-root && poetry run alembic upgrade head && poetry run uvicorn app.main:app --host 0.0.0.0 --port $PORT
