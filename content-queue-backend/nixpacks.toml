[phases.setup]
nixPkgs = ["python311", "poetry"]

[phases.install]
cmds = ["poetry sync --no-root"]

[phases.build]
cmds = ["poetry run alembic upgrade head"]

[start]
cmd = "poetry run uvicorn app.main:app --host 0.0.0.0 --port $PORT"
