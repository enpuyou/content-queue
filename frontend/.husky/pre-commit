# npm test

#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

cd frontend

# Run lint-staged for formatting and linting
npx lint-staged

# Run type check to catch TypeScript errors
echo "Running TypeScript type check..."
npm run type-check
